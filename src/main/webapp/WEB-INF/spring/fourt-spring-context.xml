<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/context
						http://www.springframework.org/schema/context/spring-context-3.2.xsd
						http://www.springframework.org/schema/beans
						http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
						http://www.springframework.org/schema/tx 
						http://www.springframework.org/schema/tx/spring-tx-3.2.xsd">

	<!-- <context:property-placeholder location="classpath:Appconfig.properties"
		ignore-resource-not-found="false" ignore-unresolvable="false"
		system-properties-mode="OVERRIDE" />

	<bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource"
		destroy-method="close">
		<property name="driverClassName" value="${jdbc.driverClassName}" />
		<property name="url"
			value="jdbc:mysql://${OPENSHIFT_HSQL_DB_HOST}:${OPENSHIFT_HSQL_DB_PORT}/${fourt.tasktracker.db.name}" />
		<property name="username" value="${OPENSHIFT_HSQL_DB_USERNAME}" />
		<property name="password" value="${OPENSHIFT_HSQL_DB_PASSWORD}" />
		<property name="initialSize" value="${jdbc.poolInitialSize}" />
		<property name="maxTotal" value="${jdbc.poolMaxTotal}" />
		<property name="maxIdle" value="${jdbc.poolMaxIdle}" />
		<property name="minIdle" value="${jdbc.poolMinIdle}" />
		<property name="maxWaitMillis" value="${jdbc.poolMaxWaitMillis}" />
	</bean>

	<bean id="sessionFactory"
		class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="mappingResources">
			<list>
			 	<value>org/mpmprof/portal/domain/Member.hbm.xml</value>
				<value>org/mpmprof/portal/domain/Address.hbm.xml</value>
				<value>org/mpmprof/portal/domain/Education.hbm.xml</value> -->
<!-- 			</list>
		</property>
		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.dialect">org.hibernate.dialect.HSQLDialect</prop>
				<prop key="hibernate.show_sql">true</prop>
				<prop key="hibernate.hbm2ddl_auto">update</prop>
			</props>
		</property>
	</bean>

	<bean id="transactionManager"
		class="org.springframework.orm.hibernate4.HibernateTransactionManager">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>
-->
</beans>

